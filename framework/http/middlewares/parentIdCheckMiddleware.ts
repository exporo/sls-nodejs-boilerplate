import jwk from "jwks-rsa";

export class jwtAuth implements middlewareInterface {
    public allow = (req) => {
        if (!model.where('id', req.params.parentId).where('user_id', this.authUser.Id){
            throw{};
        }
    };
}